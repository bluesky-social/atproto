{
  "name": "@atproto/pds-templates",
  "version": "0.0.1",
  "license": "MIT",
  "description": "Templates used by the atproto Personal Data Server (PDS)",
  "keywords": [
    "atproto",
    "pds",
    "templates"
  ],
  "homepage": "https://atproto.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/bluesky-social/atproto",
    "directory": "packages/pds-templates"
  },
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "pnpm run build:js && pnpm run build:types",
    "build:js": "node build.js",
    "build:types": "tsc --build"
  },
  "dependencies": {
    "handlebars": "^4.7.7",
    "tslib": "^2.6.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.8",
    "esbuild-plugin-handlebars": "^1.0.2"
  }
}
