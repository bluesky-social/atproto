syntax = "proto3";

package bsync;
option go_package = "./;bsync";

//
// Bsky Sync
//


// Ping
message PingRequest {}
message PingResponse {}


service Service {
  // Ping
  rpc Ping(PingRequest) returns (PingResponse);
}
