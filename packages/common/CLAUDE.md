# @atproto/common — Node-only shared utilities

Helpers that need Node APIs (streams, fs, crypto): CBOR, CID generation, async helpers, byte utilities. Anything Node-only that more than one package needs.

For browser-compatible equivalents see `@atproto/common-web`.
