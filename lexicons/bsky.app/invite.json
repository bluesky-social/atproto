{
  "lexicon": 1,
  "id": "app.bsky.invite",
  "type": "record",
  "key": "tid",
  "record": {
    "type": "object",
    "required": ["group", "subject", "createdAt"],
    "properties": {
      "group": {"type": "string"},
      "subject": {"type": "string"},
      "createdAt": {"type": "string", "format": "date-time"}
    }
  }
}