{
  "lexicon": 1,
  "id": "com.atproto.simplespace.updateSpace",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Update the configuration of a space. The authenticated user must be the space owner. Omitted fields are left unchanged. Requires auth, implemented by PDS.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["space"],
          "properties": {
            "space": {
              "type": "string",
              "format": "space-uri",
              "description": "Reference to the space to update."
            },
            "mintPolicy": {
              "type": "string",
              "knownValues": ["public", "member-list", "app-callback"],
              "description": "How the host decides whether to authorize a credential for a requesting user."
            },
            "managingApp": {
              "type": "string",
              "description": "Service identifier of the managing application. Pass an empty string to clear."
            },
            "appAccessMode": {
              "type": "string",
              "knownValues": ["allow", "deny"],
              "description": "App-level access policy."
            },
            "appExceptions": {
              "type": "array",
              "description": "Apps that break from the default appAccessMode. Sent as a full replacement, not a delta.",
              "items": {
                "type": "string",
                "description": "OAuth client ID."
              }
            }
          }
        }
      },
      "errors": [{ "name": "SpaceNotFound" }, { "name": "NotSpaceOwner" }]
    }
  }
}
