{
  "lexicon": 1,
  "id": "com.atproto.resolveName",
  "type": "query",
  "description": "Provides the DID of a repo.",
  "parameters": {
    "name": {"type": "string", "description": "The name to resolve. If not supplied, will resolve the host's own name."}
  },
  "output": {
    "encoding": "application/json",
    "schema": {
      "type": "object",
      "required": ["did"],
      "properties": {
        "did": {"type": "string"}
      }
    }
  }
}