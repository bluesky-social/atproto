{
  "lexicon": 1,
  "id": "app.bsky.graph.list-block",
  "defs": {
    "main": {
      "type": "record",
      "description": "A block of an entire list of users.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["subject", "createdAt"],
        "properties": {
          "subject": {"type": "string", "format": "at-uri"},
          "createdAt": {"type": "string", "format": "datetime"}
        }
      }
    }
  }
}
