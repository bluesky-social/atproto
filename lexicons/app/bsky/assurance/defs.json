{
  "lexicon": 1,
  "id": "app.bsky.assurance.defs",
  "defs": {
    "ageAssuranceState": {
      "type": "object",
      "required": ["required", "status"],
      "properties": {
        "required": {
          "type": "boolean",
          "description": "Whether the age assurance process is required for the account. If true, the user must complete age assurance process to access certain features."
        },
        "status": {
          "type": "string",
          "description": "The status of the age assurance process.",
          "knownValues": [
            "unknown",
            "pending",
            "assured",
            "failed"
          ]
        }
      }
    }
  }
}
