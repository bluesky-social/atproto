{
  "lexicon": 1,
  "id": "app.bsky.bookmark.deleteBookmark",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Deletes a private bookmark for the specified at-uri record. Requires authentication.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["bookmark"],
          "properties": {
            "bookmark": {
              "type": "ref",
              "ref": "app.bsky.bookmark.defs#bookmark"
            }
          }
        }
      },
      "errors": [
        {
          "name": "DifferentCid",
          "description": "A bookmark with the specified URI was found but it has a different CID than the provided one."
        },
        {
          "name": "UnsupportedCollection",
          "description": "The URI to be bookmarked is for an unsupported collection."
        }
      ]
    }
  }
}
