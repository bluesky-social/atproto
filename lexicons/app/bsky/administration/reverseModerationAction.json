{
  "lexicon": 1,
  "id": "app.bsky.administration.reverseModerationAction",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Reverse a moderation action.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["id", "reversedBy", "reversedRationale"],
          "properties": {
            "id": {"type": "integer"},
            "reversedBy": {"type": "string"},
            "reversedRationale": {"type": "string"}
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "ref",
          "ref": "app.bsky.administration.moderationAction#view"
        }
      }
    }
  }
}
