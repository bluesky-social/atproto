{
  "lexicon": 1,
  "id": "app.bsky.administration.moderationAction",
  "defs": {
    "view": {
      "type": "object",
      "required": ["id", "action", "subject", "rationale", "createdBy", "createdAt"],
      "properties": {
        "id": {"type": "integer"},
        "action": {"type": "string", "knownValues": ["takedown"]},
        "subject": {"type": "union", "refs": ["app.bsky.actor.ref"]},
        "rationale": {"type": "string"},
        "createdBy": {"type": "string"},
        "createdAt": {"type": "string"},
        "reversedBy": {"type": "string"},
        "reversedAt": {"type": "string"},
        "reversedRationale": {"type": "string"}
      }
    }
  }
}
