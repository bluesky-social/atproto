{
  "lexicon": 1,
  "id": "app.bsky.unspecced.registerPushNotification",
  "defs": {
    "main": {
      "type": "query",
      "description": "An unspecced route to register a push notification token for a user at the specified endpoint",
      "parameters": {
        "type": "params",
        "required": ["token", "platform", "endpoint", "appId"],
        "properties": {
          "token": {"type": "string"},
          "platform": {"type": "string", "enum": ["ios", "android", "web"]},
          "endpoint": {"type": "string"},
          "appId": {"type": "string"}
        }
      }
    }
  }
}
