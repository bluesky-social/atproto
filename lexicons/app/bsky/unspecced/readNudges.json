{
  "lexicon": 1,
  "id": "app.bsky.unspecced.readNudges",
  "defs": {
    "main": {
      "type": "query",
      "description": "Retrieve compressed nudges string for an account. Requires auth.",
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["nudges"],
          "properties": {
            "nudges": {
              "type": "string",
              "description": "The compressed nudges string that was stored."
            }
          }
        }
      }
    }
  }
}
