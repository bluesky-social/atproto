{
  "lexicon": 1,
  "id": "tools.ozone.moderator.deleteUser",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Delete a user from moderator's list in the ozone service. Requires admin role.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["did"],
          "properties": {
            "did": { "type": "string", "format": "did" }
          }
        }
      },
      "errors": [
        {
          "name": "ModeratorNotFound",
          "description": "The user being deleted is not a moderator"
        },
        {
          "name": "OnlyRemainingAdmin",
          "description": "There must always be at least one moderator with admin access"
        }
      ]
    }
  }
}
