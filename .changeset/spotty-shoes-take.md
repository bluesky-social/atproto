---
'@atproto/syntax': patch
---

Allow `DatetimeString` to be any valid `datetime` string, including values that are less than 24HH hours before the start of year 0000 and after the end of year 9999
